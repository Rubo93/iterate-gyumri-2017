'use strict';
const upper = (str)=> {
  return str.toUpperCase();

}

module.exports = {
  upper
}

//Fat arrow function ,Lambda function
//(arguments = >{bodu})
//thin arrow ->,fat arrow =>
//there is no thin arrow in Js
